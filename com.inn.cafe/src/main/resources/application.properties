spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/cafevideo
spring.datasource.username=root
spring.datasource.password=test

#When we want to see the actual queries in the terminal generated by hibernate after our application is run, then we use the below statement.
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#If we want the SQL queries to be formatted in the logs then we use
spring.jpa.properties.hibernate.format_sql=true

#If we want to run our project on different port then we use
server.port=8081

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=girishharagon115@gmail.com
spring.mail.password=bsdazkmbzqmqeryl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#server.shutdown=graceful
#spring.lifecycle.timeout-per-shutdown-phase=1m
